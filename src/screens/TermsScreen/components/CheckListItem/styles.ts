import styled from 'styled-components/native'

export const CheckListItemContainer = styled.TouchableOpacity`
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    margin: 5px 0;
`

export const CheckListItemSection = styled.View`
    flex-direction: row;
    align-items: center;
`
